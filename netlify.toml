[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so deep links like /projects/oncocast resolve to index.html.
# (Also mirrored in public/_redirects for redundancy.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
